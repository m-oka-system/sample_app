<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @collection, renderer: BootstrapPagination::Rails %>

<ul class="users">
<% @users.each do |user| %>
  <li>
    <%= render user %>
    <%#= render @users %>
  </li>
<% end %>
</ul>

<%= will_paginate @collection, renderer: BootstrapPagination::Rails %>